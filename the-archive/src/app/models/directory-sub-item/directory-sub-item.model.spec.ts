import { DirectorySubItem } from './directory-sub-item.model';

describe('DirectorySubItem', () => {
  it('should create an instance', () => {
    expect(new DirectorySubItem()).toBeTruthy();
  });
});
